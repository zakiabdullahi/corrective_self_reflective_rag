# Increase timeouts for PDF processing (docling + embedding + Qdrant upload)
proxy_read_timeout    300s;
proxy_connect_timeout 300s;
proxy_send_timeout    300s;

# Allow large file uploads (up to 50MB)
client_max_body_size  50m;
